type NewFarmingInfo = {
  nodeId: string;
  challengeHash: string;
  signagePoint: string;
  timestamp: number;
  passedFilter: number;
  proofs: number;
  totalPlots: number;
  lookupTime: number;
};

export default NewFarmingInfo;
